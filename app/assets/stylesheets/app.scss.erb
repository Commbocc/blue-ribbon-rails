// @import 'https://fonts.googleapis.com/css?family=Raleway:500,500i,600';
// @import 'https://fonts.googleapis.com/css?family=PT+Sans:400,400i,700,700i';
// @import 'https://fonts.googleapis.com/css?family=Ubuntu:400,400i,700,700i';

// $font-family-base: Menlo, Monaco, Consolas, "Courier New", monospace !default;
// $font-family-base: Georgia, "Times New Roman", Times, serif !default;
// $font-family-base: 'Raleway', sans-serif !default;

<% sass_dir = [Rails.root.join('app', 'assets', 'stylesheets'), '*.scss'].join('/') %>
<% Dir.glob(sass_dir).each do |file| %>
@import "<%= File.basename(file, ".scss") %>";
<% end %>
@import "./theme/*";
@import "bootstrap";
@import "font-awesome";
